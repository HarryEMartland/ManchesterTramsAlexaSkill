{
  "name": "manmet",
  "version": "1.0.0",
  "description": "Alexa sckill for tram updates in the Greater Manchester area",
  "main": "index.js",
  "scripts": {
    "build": "tsc;zip -r -j app.zip ./src/main/**.js",
    "clean": "rm ./src/**/*.js;rm ./src/**/*.js.map",
    "test": "istanbul cover _mocha ./src/test/**/*.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.7.10"
  },
  "devDependencies": {
    "@types/aws-lambda": "0.0.5",
    "@types/aws-sdk": "0.0.42",
    "@types/mocha": "^2.2.33",
    "@types/node": "0.0.2",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "typescript": "^2.1.4"
  }
}
